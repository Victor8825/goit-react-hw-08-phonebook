import { Typography } from '@mui/material';

const Home = () => {
  return (
    <Typography
      variant="h1"
      component="h1"
      align="center"
      mt={4}
      color="secondary"
    >
      Contacts App welcome page
    </Typography>
  );
};

export default Home;
